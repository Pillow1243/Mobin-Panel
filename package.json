{
  "name": "mobin-panel",
  "version": "1.0.0",
  "description": "Mobin Panel — next-generation, open-source Cloudflare Worker configuration panel for VLESS, Trojan and Warp protocols. Created by Mobin.A",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "Mobin.A",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node tools/local-dev.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && wrangler deploy",
    "deploy:prod": "npm run build && wrangler deploy --env production"
  },
  "dependencies": {
    "@noble/curves": "^2.0.1",
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.2",
    "vitest": "^3.2.7",
    "wrangler": "^4.22.0"
  }
}
